project_include_dirs += [
    include_directories('argtable3'),
]

project_source_files += [
    files('argtable3/argtable3.c'),
]

project_header_files += [
    files('argtable3/argtable3.h'),
]


# ------------------------------------------------------------------------------
# Testing infrastructure files
# ------------------------------------------------------------------------------
# We compile these separately for testing

test_include_dirs += [
    include_directories('unity'),
]

test_source_files += [
    files('unity/unity.c')
]

test_header_files += [
    files('unity/unity.h'),
]
